services:
  bluetooth-speaker:
    build: .
    container_name: bluetooth-speaker
    network_mode: host
    privileged: true
    volumes:
      - /dev/snd:/dev/snd
      - ./bt-bonds:/var/lib/bluetooth
    environment:
      SPEAKER_NAME: "Any Phone Output"
    restart: unless-stopped
